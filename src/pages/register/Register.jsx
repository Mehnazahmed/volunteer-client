import { Box } from '@mui/material';
import React from 'react';

const Register = () => {
    return (
        <Box>
            register
            
        </Box>
    );
};

export default Register;